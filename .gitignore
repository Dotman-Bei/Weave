__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.DS_Store

# Local graph state
data/weave.db
data/weave.db-wal
data/weave.db-shm

# Benchmark data is downloaded at runtime
data/longmemeval/
data/locomo/

# Benchmark *results* are checked in: the README quotes them, so they have to
# be verifiable. Only the verbose run logs stay out.
results/*.log
