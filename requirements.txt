# Vercel's Python runtime installs from this file; it does not read
# pyproject.toml's optional-dependency extras. Keep this the *serving*
# dependency set only -- benchmarks, tests and the Bolt backend are not
# installed in the deployment.
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
